{
  "name": "my-name-is-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export -o ../build",
    "export": "next export -o ../build",
    "server": "yarn build && http-server ./build -p 8080"
  },
  "dependencies": {
    "next": "10.0.6",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "http-server": "^0.12.3"
  }
}
